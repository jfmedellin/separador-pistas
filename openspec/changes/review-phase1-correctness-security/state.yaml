change: review-phase1-correctness-security
project: separador-pistas
artifact_store: hybrid
execution_mode: auto
delivery_strategy: ask-on-risk
review_budget_lines: 800
source: PROJECT_REVIEW_2026-08-30.md (Fix first #1-3: SEC-01, ARC-01, ARC-02)
phases:
  explore: done
  proposal: done
  spec: done
  design: done
  tasks: done
  apply: phase-a-and-b-done (C blocked on external SHA-256 manifest)
  verify: pending
  archive: pending

paused_at: 2026-08-31
resume_with: |
  Working tree has Phase A (ARC-01) + Phase B (ARC-02) implemented and uncommitted,
  398/398 Tests/Portable tests passing (independently re-run and confirmed by the
  orchestrator, not just the apply sub-agent). Nothing has been committed or pushed.
  To resume:
    1. Decide whether to commit/PR Phase A and Phase B now (as two stacked PRs,
       per design's stacked-to-main strategy) before starting Phase C, or keep
       working in the same tree.
    2. Phase C (SEC-01, tasks C.1-C.14) is blocked on an out-of-band prerequisite:
       trusted full SHA-256 digests for the real Demucs `htdemucs` and `htdemucs_6s`
       weight files. These must come from downloading the genuine files from
       Demucs' own trusted source and computing/verifying the hashes -- not
       something any agent should fabricate. Resolve this before continuing Phase C.
    3. Phase C's two non-blocked apply-time verification tasks (C.1: does the
       frozen worker forward `--repo` verbatim; C.2: does PyInstaller collect
       `demucs/remote/*.yaml`) can be done independently of the manifest if useful.
    4. All artifacts (proposal, specs, design, tasks, apply-progress) are mirrored
       in Engram under topic keys `sdd/review-phase1-correctness-security/*` and
       in this repo under `openspec/changes/review-phase1-correctness-security/`.
