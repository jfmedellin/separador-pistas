change: review-phase1-correctness-security
project: separador-pistas
artifact_store: hybrid
execution_mode: auto
delivery_strategy: ask-on-risk
review_budget_lines: 800
source: PROJECT_REVIEW_2026-08-30.md (Fix first #1-3: SEC-01, ARC-01, ARC-02)
phases:
  explore: done
  proposal: done
  spec: done
  design: done
  tasks: done
  apply: done (A+B+C all committed 2026-09-03)
  verify: pending
  archive: pending

paused_at: 2026-09-03
branch: fix/review-phase1-correctness-security (local only, not pushed)
commits:
  - f434fc0 docs: add external project review and Phase 1 SDD plan
  - 854df88 fix: enforce single instance and immutable job input (ARC-01, ARC-02)
  - edf0d9b fix: app-owned model manager with verified SHA-256 manifest (SEC-01)
resume_with: |
  All three phases (ARC-01, ARC-02, SEC-01) are committed (3 commits, single
  local branch fix/review-phase1-correctness-security, not pushed, no PR
  opened yet) -- the user chose one combined branch instead of the design's
  originally suggested three-stacked-PR split. 411/411 Tests/Portable tests
  passing (398 baseline + 13 new for SEC-01), independently re-run and
  confirmed by the orchestrator against .venv-portable.

  To resume:
    1. Run sdd-verify against the full change (spec/design/tasks vs.
       implementation) before archiving.
    2. Decide whether to push this branch / open a PR now.
    3. Documented apply-time deviations (see tasks.md's Phase B/C sections):
       Phase B reads metadata from the original path, not the staged copy
       (cosmetic, spec-compliant); Phase C's design cited
       `runtime/worker_main.py` for the frozen worker but the real file is
       `demucs_worker.py`; model_manager.ensure_model() gained an extra
       `bundled_root` test-injection parameter; separate_audio() gained
       `on_progress`; SplitLibraryController gained `on_model_progress`.
    4. All artifacts (proposal, specs, design, tasks, apply-progress) are mirrored
       in Engram under topic keys `sdd/review-phase1-correctness-security/*` and
       in this repo under `openspec/changes/review-phase1-correctness-security/`.

  This change (SEC-01/ARC-01/ARC-02) is "Fix first" #1-3 / delivery-sequence
  Phase 1 of PROJECT_REVIEW_2026-08-30.md. Phase 2 (ARC-03 bounded/cancellable
  job lifecycle + ARC-04 wire the tested-but-unused runtime/supervisor.py, or
  remove it) is the next recommended change and has not been started.
